#Top down approach, let's say if there are total of 6 blocks then, if Layer 1= 1 block, Layer 2= 2 block and Layer 3= 3 blocks
blocks = int(input("Enter the number of blocks: "))

height=0

while blocks>height:
    height= height + 1
    blocks= blocks - height
    
    
print("The height of the pyramid is:", height)
